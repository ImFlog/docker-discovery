server.port: 8080

spring.application.name: poule

eureka:
  instance:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/

feign.hystrix.enabled: false
ribbon.ReadTimeout: 6000